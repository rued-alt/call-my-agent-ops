# LOCAL DEV ONLY — never commit a populated .env (gitignored).
# In production, set these in the Render dashboard.

# Clerk publishable key — same key as the customer app for now (single
# Clerk instance, role check done via publicMetadata.role).
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...

# Backend base URL (Go-Live API, same as customer app).
VITE_OPS_API_BASE=https://api.callmyagent.ai
